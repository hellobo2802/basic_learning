#include<iostream>
using namespace std;
#include<cmath>
int main()
{
    cout << "??????????????¡ì????????~" << endl;
    int a, b, c, num = 100;
    do
    {
        // ??????
        c = num % 10; // ??????
        b = (num / 10) % 10; // ?????
        a = (num / 100); // ??????

        // a^3????
        // (int)pow(a,3)+(int)pow(b,3)+(int)pow(c,3)???¡ì??pow??a,b????????double?????????????int????????
        if(a*a*a + b*b*b + c*c*c == num)  // a^3 ???¡ì??????
            cout << num << endl;
        num++;
    }while(num < 1000);
    system("pause");
    return 0;
}